{{ if .Values.pglocal }} 
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pglocal-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
       storage: 100Gi
{{ end }}
