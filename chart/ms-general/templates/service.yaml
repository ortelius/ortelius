apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-general.name" . }}
spec:
  selector:
    app: {{ include "ms-general.name" . }}
    tier: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
---
 
