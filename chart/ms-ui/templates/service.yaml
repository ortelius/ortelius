apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-ui.name" . }}
spec:
  selector:
    app: {{ include "ms-ui.name" . }}
    tier: backend
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30091
  type: NodePort
---
 
